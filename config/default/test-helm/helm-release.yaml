apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-helm
  namespace: default
  labels:
    app.kubernetes.io/part-of: test-iterate
    apps.tanzu.vmware.com/workload-type: helm
    app.kubernetes.io/component: helm-release
    app.tanzu.vmware.com/release-type: helm
spec:
  interval: 5m
  chart:
    spec:
      chart: java-web-app
      version: 0.1.1
      sourceRef:
        kind: HelmRepository
        name: demo
        namespace: default
      interval: 1m
  upgrade:
    remediation:
      remediateLastFailure: true
  values:
    autoscaling:
      enabled: true
      minReplicas: 3
    image:
      pullPolicy: IfNotPresent
      repository: harbor.vrabbi.cloud/tap/test-helm-default@sha256:f871658fa683e1faf9a84095ae492dc3483bfe577a61e8400d1801f6a77454b4
    replicaCount: 5

