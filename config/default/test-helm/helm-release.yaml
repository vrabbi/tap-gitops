apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: test-helm
  namespace: default
  labels:
    app.kubernetes.io/part-of: test-iterate
    apps.tanzu.vmware.com/workload-type: helm
    app.kubernetes.io/component: helm-release
    app.tanzu.vmware.com/release-type: helm
spec:
  interval: 5m
  chart:
    spec:
      chart: java-web-app
      version: 0.1.0
      sourceRef:
        kind: HelmRepository
        name: demo
        namespace: default
      interval: 1m
  upgrade:
    remediation:
      remediateLastFailure: true
  values:
    autoscaling:
      enabled: true
      minReplicas: 3
    image:
      pullPolicy: IfNotPresent
      repository:
        image: harbor.vrabbi.cloud/tap/test-helm-default@sha256:a96ae5e7fc37f6b75c496ecdd75b9e9f63310371b0c248e8fccb408cdcfa8890
        name: image
    replicaCount: 5

